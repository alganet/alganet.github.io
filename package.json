{
  "name": "alganet.github.io",
  "version": "0.0.0",
  "private": true,
  "description": "Site sources. npm covers only terminal/'s browser dependencies — the site itself is generated by build.sh, in POSIX shell, with no toolchain.",
  "license": "ISC",
  "scripts": {
    "build:terminal": "node tools/build-terminal.mjs",
    "build": "npm run build:terminal"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "wasi-sh": "^0.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
