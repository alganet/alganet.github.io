/drafts

# npm — only terminal/'s browser dependencies live here; the site itself is
# generated by build.sh with no toolchain at all.
node_modules/

# built by tools/build-terminal.mjs, deployed by .github/workflows/deploy.yml
/terminal/dist/
